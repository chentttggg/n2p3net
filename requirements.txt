# N2P3-Net 依赖清单
# 目标环境：Python 3.14.x（已逐项核实对 3.14 的 wheel 支持，2026-08 最新版本）
# 若遇到某个包报 "Requires-Python <3.14"，先执行：python -m pip install --upgrade pip

# --- 数据层（信号处理） ---
numpy>=2.0            # 解析到 2.5.x（含 cp314 wheel）
scipy>=1.14           # 解析到 1.18.x（含 cp314 wheel）
mne>=1.8              # 纯 Python（py3-none-any），1.12.x 已支持 3.14
matplotlib>=3.9       # mne 依赖；3.11.x 起有 cp314 wheel

# --- 模型层（深度学习） ---
# torch 须按设备选源单独安装（PyPI 官方 wheel 为 CPU 版，不带 CUDA/XPU），详见 doc/device-portability.md §7：
#   NVIDIA 5070   : pip install torch --index-url https://download.pytorch.org/whl/cu132
#                   （Blackwell sm_120 需 CUDA≥12.8；Python 3.14 需 torch 2.13，cu128 索引只到 2.11）
#   Intel Arc 130T: pip install torch --index-url https://download.pytorch.org/whl/xpu
#   CPU 兜底      : pip install torch
torch>=2.4            # 解析到 2.13.x（含 cp314 wheel）；先按上述源装 GPU 版，再 pip install -r requirements.txt
braindecode>=1.7      # EEG 深度学习基线库（EEGNet/DeepConvNet/ATCNet）；纯 Python，1.7.0 起明确支持 3.14

# --- 数据处理与基线 ---
pandas>=2.2           # 解析到 3.0.x（含 cp314 wheel）
scikit-learn>=1.5     # 解析到 1.9.x（含 cp314 wheel）
pyriemann>=0.6        # xDAWN + 黎曼几何基线；纯 Python（py3-none-any）
moabb>=1.1            # 公开 P300 数据下载/缓存（experiments/download_datasets.py 依赖）

# --- 实验管理 ---
hydra-core>=1.3       # 纯 Python；若出现 ReadTimeout 属网络问题，重试即可

# --- 开发与测试 ---
pytest>=8.0           # 纯 Python（py3-none-any）

# --- 代码质量工具 ---
ruff>=0.14            # Python 3.14 支持自 0.14.0 起；最新 0.16.3 明确标注 Python :: 3.14
black>=25.11          # Python 3.14 支持自 25.11.0 起；最新 26.5.1（Requires-Python >=3.10，无上限）
                      # 注：ruff format 已可替代 black，二选一即可，二者保留其一

# --- 可选：实验追踪（二选一） ---
# wandb
# mlflow
